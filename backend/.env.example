# MongoDB Atlas connection string
# Replace placeholders with your actual credentials in the .env file
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-address>/<database-name>

# Port the backend server will listen on
PORT=5000

# Allowed origin for CORS (frontend URL). Use * for development if needed.
ALLOWED_ORIGIN=http://127.0.0.1:5500